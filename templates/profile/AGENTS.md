# Profile Instructions

This is a working profile containing registered repositories below `projects/`.
Keep profile-wide knowledge in this profile and repository-specific work in the
selected repository.

For a repository named `<repo-id>` in `PROJECTS.toml`, keep materially changed
status and unfinished work in `project-context/<repo-id>/STATUS.md` and
`TASKS.md`, and keep decisions there. Do not create or update harness documents
inside `projects/<repo>/`; follow that repository's own instructions for code
work.

Treat `DASHBOARD.md` as a generated overview, not a source of truth. Do not edit
`.harness/` directly unless the user explicitly requests harness administration.
