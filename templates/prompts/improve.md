# Profile improvement proposals

Review only the supplied bounded curated profile state and curation journal metadata.
Return JSON conforming to the supplied schema. Every proposal must cite one or more
supplied curation journal hashes. Propose reviewable Markdown guidance only. Do not
request, describe, or emit filesystem paths, repository edits, policy-file writes,
configuration changes, accepted changes, rejected changes, or automatic application.
